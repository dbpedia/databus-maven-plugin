<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.dbpedia.databus.Termilion</groupId>
    <artifactId>generic</artifactId>
    <packaging>pom</packaging>
    <version>1.0.0</version>
    <modules>
        <module>infobox-properties</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.7</maven.compiler.source>
        <maven.compiler.target>1.7</maven.compiler.target>
    </properties>


    <pluginRepositories>
        <pluginRepository>
            <id>archiva.internal</id>
            <name>Internal Release Repository</name>
            <url>http://databus.dbpedia.org:8081/repository/internal</url>
        </pluginRepository>
        <pluginRepository>
            <id>archiva.snapshots</id>
            <name>Internal Snapshot Repository</name>
            <url>http://databus.dbpedia.org:8081/repository/snapshots</url>
        </pluginRepository>
    </pluginRepositories>
    <!--repositories>
        <repository>
            <id>archiva.internal</id>
            <name>Internal Release Repository</name>
            <url>http://databus.dbpedia.org:8081/archiva/repository/internal/</url>
        </repository>
        <snapshotRepository>
            <id>archiva.snapshots</id>
            <name>Internal Snapshot Repository</name>
            <url>http://databus.dbpedia.org:8081/archiva/repository/snapshots/</url>
        </snapshotRepository>
    </repositories-->


    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.dbpedia.databus</groupId>
                <artifactId>databus-maven-plugin</artifactId>
                <version>1.0-SNAPSHOT</version>
                <executions>
                    <execution>
                        <id>validate</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>validate</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>analysis</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>analysis</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <!--dataDirectory
                         the input directory where the datafiles are
                         Note: that per convention
                         * only files will be included that have the same prefix as the artifactId
                         * any dataid files from previous runs will also be ignored
                         Default: src/main/databus/
                    -->
                    <!--dataDirectory>/home/shellmann/IdeaProjects/data-release-tool-ti/dbpedia/20180420</dataDirectory-->
                    <dataDirectory>src/main/databus/</dataDirectory>


                    <!--pluginDirectory
                        all the generated files will be written here, i.e. parselogs, dataids, feeds
                        Default: ${project.build.directory}/databus/
                        (i.e. target/databus/)
                    -->
                    <pluginDirectory>${project.build.directory}/databus/</pluginDirectory>

                    <!--dataIdDirectory
                        the directory where the dataids are created
                        this can theoretically be the same as the dataDirectory, although it is recommended to keep it separate
                        Default: ${project.build.directory}/databus/${project.artifactId}/${project.version}/
                        (i.e. target/databus/releases/artifactid/x.y.z
                    -->
                    <dataIdDirectory>${project.build.directory}/databus/releases/${project.artifactId}/${project.version}/</dataIdDirectory>


                    <!--maintainer>https://termilion.github.io/webid.ttl#this</maintainer-->
                    <maintainer>http://kurzum.net/dev-dummy.ttl#this</maintainer>
                    <privateKeyFile>private_key_dev_dummy.der</privateKeyFile>
                    <issuedDate>2018-08-01</issuedDate>
                    <modifiedDate>2018-08-03</modifiedDate>
                    <license>http://purl.oclc.org/NET/rdflicense/cc-by-sa3.0</license>

                </configuration>
            </plugin>
        </plugins>
    </build>


</project>
